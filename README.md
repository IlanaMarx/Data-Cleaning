# Data-Cleaning

This project uses the pandas and fuzzywuzzy libraries to clean the store_income_data_task dataset. The store_income_data_task.csv file contains the data used for the data cleaning. This project includes importing the dataset, cleaning the "country" column to guarantee consistency and parsing the "date_measured" column to generate a new "days_ago" column that shows the number of days from the current date.
